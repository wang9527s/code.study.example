---------------------------------------------------------------------
init函数
    一个包中可能有多个init函数
    init函数在main函数之前执行

---------------------------------------------------------------------
chan 通道
    发送数据：通道变量 <- 值
    接受数据：

1. 通道的收发操作在不同的两个 goroutine 间进行。
    由于通道的数据在没有接收方处理时，数据发送方会持续阻塞，因此通道的接收必定在另外一个 goroutine 中进行。
2. 接收将持续阻塞直到发送方发送数据。
    如果接收方接收时，通道中没有发送方发送数据，接收方也会发生阻塞，直到发送方发送数据为止。
---------------------------------------------------------------------


切片传参和引用传参的区别
    切片传参是值传递，复制的时候是浅拷贝;所以看起来像引用传参
    
type Cache struct {
    m map[string]string     //成员变量
    sync.Mutex          //继承
}

defer 在return之后执行，后写的先执行

slices  /  map / channels           使用make
struct                              使用new创建

---------------------------------------------------------------------

go的包管理（导入包）

第十章
    A：聚合（或组合）：包含一个所需功能类型的具名字段。
    B：内嵌：内嵌（匿名地）所需功能类型，像前一节 10.6.5 所演示的那样。
    

---------------------------------------------------------------------
go func(){}()                               //以并发的方式调用匿名函数func

strings.HasPrefix(flatpakRef, "app/")       //字符串是否以指前缀开头
strings.EqualFold(s, t string) bool            // 判断两个utf-8编码字符串，大小写不敏感

